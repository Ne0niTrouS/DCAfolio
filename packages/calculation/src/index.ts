/**
 * Pure, decimal-safe financial math for DCAfolio.
 *
 * No React, no Supabase, no I/O. Everything here must be testable with plain
 * data so a future API or React Native client can reuse it unchanged.
 *
 * Phase 1 ships this package as an empty, installable workspace. The decimal
 * foundation and the portfolio engine are built test-first in Phase 4 — see
 * docs/plans/implementation-plan.md.
 */
export {};
