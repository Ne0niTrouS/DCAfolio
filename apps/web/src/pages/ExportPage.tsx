/** Placeholder. CSV and XLSX export is built in Phase 9. */
export function ExportPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold tracking-tight text-ink">Export</h1>
      <p className="mt-2 text-sm text-ink-muted">CSV and XLSX export arrive in Phase 9.</p>
    </section>
  );
}
